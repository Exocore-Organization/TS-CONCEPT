---
name: ts-all
description: "Load all TypeScript coding standards at once. Reads every skill under .agents/skills/ so all rules are applied."
trigger: /ts-all
---

# /ts-all

Read every skill folder under `.agents/skills/` one by one.
